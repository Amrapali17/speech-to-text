{
  "name": "speech-to-text-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "postinstall": "apt-get update && apt-get install -y ffmpeg python3 python3-pip && pip3 install --no-cache-dir -r backend/requirements.txt && node download-model.js",
    "start": "node download-model.js && node server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "cors": "^2.8.5",
    "express": "^4.19.0",
    "ffmpeg-static": "^5.2.0",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
